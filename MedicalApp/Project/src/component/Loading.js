import React from 'react';
import { Buttom, TextInput, ActivityIndicator } from 'react-native';

const LoadingScreen = () => {
    return (
        <>
            <ActivityIndicator size='large' color='blue'/>
        </>
    );
}

export default LoadingScreen;